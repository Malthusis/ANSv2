export enum FlagEnum {
    FIRE_STARTED = 1, //Fire Started
    INITIAL_EXPLORATION
}

export enum Panel {
    BONFIRE = "bonfire",
    EXPLORE = "explore"
}

export enum Resource {
    FABRIC_STRIP = 1,
    SCRAP_METAL,
    CHEMICALS,
    CIRCUITS
}